#include "main.h"

/**
 * _abs - computes the absolute value of an int
 *
 * @n: stores the value of the integer
 *
 * Return: -n if negative an n if positive
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
		return (n);
}
