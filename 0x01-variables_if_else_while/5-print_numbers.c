#include <stdio.h>

/**
 * main - function to print numbers of base 10
 *        followed by the ney line
 *
 * Return: 0
 */

int main(void)
{

	int base;

		for (base = 0 ; base < 10 ; base++)
			printf("%d", base);

		printf("\n");

	return (0);
}
