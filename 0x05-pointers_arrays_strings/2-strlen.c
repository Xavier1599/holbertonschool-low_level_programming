#include "main.h"

/**
 * _strlen - function that returns the length of a string.
 *
 * @s: parameter
 *
 * Return: length
 */

int _strlen(char *s)

{
	int length = 0;

	for (length = 0; s[length] != '\0'; ++length)

	return (length);
}
